spring:
  application:
    name: RATE-LIMITER
rate:
  limiter:
    request-limit: ${REQUEST-LIMIT:3}
    time-window-min : ${TIME-WINDOW-MIN:3}
    capacity-per-slot: ${CAPACITY-PER-SLOT:6}
logging:
  level:
    web: DEBUG